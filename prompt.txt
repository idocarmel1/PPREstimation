I want to re-write the old file "create_pprs_excels.py", which is now badly-written and very partial.
this file should contain a function that runs through a directory of ewe_jsons, and for each json (from now on called "model") it creates a separate excel file with multiple sheets (as many as you think needed).
the information I want to save is:
- groups_df sheet: all relevant information on model groups for calculating the sppr and ppr and npp.
- sppr table sheet: (per group, per basal source, per calculation method) for every sppr-calculation method. per-method, it should contain also the sum of PP sources, the sum of inner sources, and the sum of all sources.
- model health sheet: contains output from diagnose_sppr(), with pretty formatting. should contain info per TE_option, and always run with: 
    'det_open_mode': 'none',
    'det_theta': 1.0,
    'det_external_sppr': 0.0,
    'det_collapse_mode': 'never',
- footprint sheet: columns are taken from report['footprint'], and index is built from the names of selected calculation methods (with their selected parameters where necessary).

the sppr calculation methods I want to run are (give them indicative names):
1. model.SPPR_1986()
2. model.SPPR_1995(global_TE=0.1)  # This is the 1995 model
4. model.SPPR_1995(global_TE='mean') # jensen-able version because it uses the true mean
5. model.SPPR_EwE_Ulanowicz(TE_option='global', global_TE='mean', use_EE=False)  # Jensen on TL
6. model.SPPR_EwE_Ulanowicz(TE_option='TE', global_TE=None, use_EE=False)  # another jensen on TE
7. model.SPPR_EwE(TE_option='TE', use_EE=True, return_paths=True, silent=False)  # This is the EwE model
8. model.SPPR_2015(only_pp_det=True)  # this is 2015's model, includes cycles
9. model.SPPR_new(TE_option='TE', fix_EE_0_cases=False)  # should reproduce 2015's model
9. model.SPPR_new(TE_option='TE', fix_EE_0_cases=True)  # 2015's model with fixed EE0 fix_EE_0_cases
10. model.SPPR_new(TE_option='GE')  # should be lower than TE
11. model.SPPR_new(TE_option='With Egestion')  # should be lower than GE
12-18. model.SPPR_symbolic(TE_option=<TE, GE, With Egestion>, diet_import_option=<as_PP, as_DC>)  # run all 6 options
19. model.monte_carlo_SPPR(
                    n_samples=100, TE_error_percent=10, TE_error_cut_percent=20, kind='new', TE_option='GE', silent=False
                )  # don't include runs where report['divergence']['status'] FAILED.
20. model.monte_carlo_SPPR(
                    n_samples=100, TE_error_percent=10, TE_error_cut_percent=20, kind='new', TE_option='TE', silent=False
                )  # use fix_EE_cases=True, and don't include runs where report['divergence']['status'] FAILED.

log a full report of the run, that includes (in an organzied and humen-readable way) all warnings and other things you think I should know before using the results of the runs.

feel free to re-organize the excel per model if you think it would be better.
the final excel should be readable and well-organized.
you should also create a function that reads this excel and loads all relevant data from it.

the ewe_jsons directory it would run on first will be "real_models/EwE_jsons".
you should load models through:
    model_data = ModelData(model_path)
    model = PPRCalculator.from_modeldata(model_data, underdetermined=True, zero_biomass_accum=False)

don't run the full thing for tests, because SPPR_EwE() is an especially heavy function.
however, percision is key.
you can start.